# 12-factor config: read everything from the environment, with safe defaults.
# Spring Boot maps the SERVER_PORT env var to this property automatically,
# so you usually don't even need this line — it's here to show the default.
server.port=${SERVER_PORT:8080}

spring.application.name=devops-practice-java-maven
